class Solution:
    def arrangeCoins(self, n: int) -> int:
        
        i,j = 0,1
        arr = []

        while (i+j) <= n :
            total = i+j 
            if  == n:
                return j
            arr.append(sums)
            i = sums
            j += 1
        arr.append(i)
        return len(arr)-1 #Better Solution need to find